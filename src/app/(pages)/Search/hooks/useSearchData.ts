import React from "react";

type Props = {};

const useSearchData = (props: Props) => {
    const user =['sadfds','sfdafdsf']
    const newUsers =['sadfds','sfdafdsf']

  return {
    user,
    newUsers,
  }
};

export default useSearchData;
